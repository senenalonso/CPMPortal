json.extract! project, :id, :code, :name, :description, :result, :date_requested, :date_start, :date_finish, :scope, :category, :priority, :created_at, :updated_at
json.url project_url(project, format: :json)